/** How long a watch stays active before requiring extension. */
export const WATCH_DURATION_MS = 24 * 60 * 60 * 1000;

/** Minimum interval between manual status refreshes per user+player. */
export const WATCH_REFRESH_COOLDOWN_MS = 60 * 1000;
